<Page x:Class="GymDesktopApp.Register"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GymDesktopApp"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Register"
      Background= "GhostWhite" >
    <Page.Resources>
        <Style x:Key="PasswordLabel" TargetType="{x:Type Label}">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Top"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Label}">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height =" Auto"/>
            <RowDefinition Height =" Auto"/>
            <RowDefinition Height =" *"/>
            


        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Rectangle Grid.Row="0" 
                   Grid.Column="0"
                   Fill="CornflowerBlue" 
                   HorizontalAlignment="Stretch" 
                   VerticalAlignment="Stretch"
                   Grid.RowSpan="4" 
                   Width="250"/>
        <TextBlock Grid.Row="0" Grid.Column="1" Text="REGISTER" FontSize="24" FontWeight="Bold" Margin="80,20,0,0" HorizontalAlignment="Left" />
        <TextBlock Grid.Row="1" Grid.Column="1" Text="Username:" FontSize="16" FontWeight="Bold" Margin="80,101,0,0" HorizontalAlignment="Left"/>
        <Label  Height="30 " Width="200  " Grid.Row="1" Grid.Column="1" Margin="80,130,0,0" HorizontalAlignment="Left" Background="GhostWhite" BorderThickness="1" BorderBrush="black"/>
        <TextBlock Grid.Row="2" Grid.Column="1" Text="Password:" FontSize="16" FontWeight="Bold" Margin="80,50,0,0" HorizontalAlignment="Left"/>
        <Label Style="{DynamicResource PasswordLabel}"  Height="30 " Width="200  " Grid.Row="2" Grid.Column="1" Margin="80,80,0,0" HorizontalAlignment="Left" Background="GhostWhite" BorderThickness="1" BorderBrush="black"/>
        
    </Grid>
</Page>
